import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'
import React from 'react'

export default function StationBookingsCard() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Stations</CardTitle>
                <CardDescription>A list of individual station bookings</CardDescription>
            </CardHeader>
            <CardContent>

            </CardContent>
        </Card>
    )
}
