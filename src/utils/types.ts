export interface SignupFormData {
    firstName: string;
    lastName: string;
    schoolID: string;
}

export interface OtpFormData {
    otp: string;
}

export interface LoginFormData {
    schoolID: string;
}